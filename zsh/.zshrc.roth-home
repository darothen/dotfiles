#!/usr/bin/env zsh
# Machine-specific ZSH configuration for: roth-home (Linux home server)
# This file is sourced by the base .zshrc - do NOT source base here

# ============================================================================
# Machine-Specific Configuration: roth-home
# ============================================================================

# Set up autocompletions
autoload -U compinit && compinit

# Local bin environment
safe_source "$HOME/.local/bin/env" "local bin environment"

# Google Cloud SDK (custom installation)
safe_source "$HOME/software/google-cloud-sdk/path.zsh.inc" "Google Cloud SDK path"
safe_source "$HOME/software/google-cloud-sdk/completion.zsh.inc" "Google Cloud SDK completion"

# pixi PATH
safe_add_path "$HOME/.pixi/bin" "append" false

# jj (Jujutsu) completions - conditional on installation
if [[ -f "/usr/local/bin/jj" ]]; then
    source <(COMPLETE=zsh jj)
fi

# Claude Code alias
alias claude="$HOME/.claude/local/claude"
